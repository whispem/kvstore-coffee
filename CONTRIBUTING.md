# Contributing to kvstore-coffee

- Clone the repo and run `npm install -g coffeescript`
- Use `npm start` and `npm test` to run the project and tests.
- Add tests for your new features in `test/` if possible.
- Follow the current code style (2 spaces, single quotes, semicolon free).
- PRs and issues are welcome!
